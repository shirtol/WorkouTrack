import styled from "styled-components";
import { Colors } from "../../utils/colors";

export const StyledPlaylistCreationStep = styled.i`
    width: 2rem;
    height: 2rem;
    color: ${Colors.blackText};
    padding: 2rem;
    align-self: center;
    cursor: pointer;
`;
